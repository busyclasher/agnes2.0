# Local browser-to-backend URL. Leave unset on Vercel to use the same-origin proxy.
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# Vercel-only server-side rewrite target, for example:
# https://safepoint-api-your-team.vercel.app
BACKEND_ORIGIN=
